System Library:

System.Print(string);
System.PrintLine(string);

System.GetAllFilesWithExt(dir, ext);
System.GetDirectoryPath(); - Gets the current working directory of the app
System.GetCurrentPath(relativePath); - Combines the current directory and the relative path

General:

Mode enum = {
    Unknown,
    Windows,
    Linux,
}

Config API:

config table = {
    Compiler = string,
    CompilerOnlySwitch = string,
    Packer = string,
}

Config.AddConfig(mode, config);

Project API:

file table = {
    SourceFile = string,
}

ProjectType enum = {
    None = 0,
    Executable = 1,
    StaticLibrary = 2,
}

project table = {
    Name = string,
    Files = array[file table],
    Type = project_type,
}

Project.AddProject(project);